# PRD — 서남권 신혼부부 전세 대시보드

> 작성일: 2026-09-03 · 상태: 초안(v0.2) · 작성: 인터뷰 기반
>
> v0.2 변경: 반전세 임계값 1억 확정 · 지도 타일 OSM 확정 · 동별 인프라 문구 초안(`data/dong-info.json`) 작성

---

## 1. 배경 / 문제

이번에 결혼하는 신혼부부가 **서울 서남권에서 신혼집(전세)을 알아보는 중**이다.
네이버부동산·호갱노노 등은 매물이 많지만, "우리 예산(전세 3억)으로 **어느 동에 갈 수 있는지**"를
지도 위에서 한눈에 비교하기 어렵다. 특히:

- 예산 필터가 매매/전세/월세 섞여 있어 비교가 번거롭다.
- 동별 시세 감(感)이 없어 "여기가 우리 예산으로 되는 동네인가?"를 빠르게 못 거른다.
- 반전세도 후보인데 보증금·월세를 같이 놓고 보기 어렵다.

**이 대시보드는 "실거래 데이터로, 우리 예산에 맞는 동네를 지도에서 빠르게 좁히는" 도구다.**
개별 매물 계약 도구가 아니라 **지역(동네) 탐색·후보 압축** 도구다.

## 2. 목표 / 비목표

### 목표
- 서남권 4개 구(구로·금천·영등포·관악)의 아파트 **전세·반전세 실거래**를 지도에 표시한다.
- **보증금 3억 이하** + 월세 상한 + 평형 프리셋으로 필터링한다.
- 동 단위로 "우리 예산에 맞는 곳인지"를 색/마커로 즉시 판별하게 한다.
- 다른 신혼부부도 URL만 열면 쓸 수 있게 **공개 웹페이지**로 배포(1차: 단일 HTML, 이후 GitHub Pages).

### 비목표 (이번 버전에서 안 함)
- 현재 "나와 있는" 매물 연동 (데이터는 **과거 실거래 내역**임).
- 매물 문의·중개·알림 기능.
- 로그인/회원가입/찜하기 등 개인화.
- 단지별 정확한 지하철 도보시간·학군·평판 (동 단위 대략 표기까지만).
- 매매 시세 분석.

## 3. 사용자

| 구분 | 설명 |
|---|---|
| 1차 사용자 | 작성자 부부 — 서남권 전세 예산 3억, 소형~중형 아파트 탐색 |
| 2차 사용자 | 비슷한 조건의 다른 신혼부부 (공개 링크로 유입, 설명 없이도 쓸 수 있어야 함) |

사용자는 **개발 지식이 없고**, 부동산 용어(전용면적, 보증금, 반전세)는 이해한다.

## 4. 데이터

### 소스
`seoul-apt-latest.csv`
(github.com/ggplab/claude-playbook · `01-hanbit-claude-guidebook/chap5/seoul-apt-latest.csv`)

- 국토교통부 아파트 실거래가 기반 추정.
- 약 320,000행, 기간 **2025-07 ~ 2026-06 (12개월)**.
- 서남권 4개 구 전세 실거래: 약 15,000건 (구로 4.8k · 금천 1.5k · 영등포 5.9k · 관악 3.0k).

### 스키마

| 컬럼 | 의미 | 타입 | 비고 |
|---|---|---|---|
| `contract_ym` | 계약 연월 | `YYYY-MM` | 최근 N개월 필터에 사용 |
| `contract_date` | 계약일 | `YYYY-MM-DD` | 일자가 1일로 몰려 있을 수 있음(월 단위 신뢰) |
| `gu` | 자치구 | 한글 | 예: 구로구 |
| `dong` | 법정동 | 한글 | 예: 신도림동 |
| `complex` | 단지명 | 한글 | |
| `area_m2` | 전용면적 | 숫자(㎡) | 평 = ㎡ ÷ 3.3058 |
| `floor` | 층 | 정수 | |
| `deal_type` | 거래유형 | `매매`\|`전세`\|`월세` | **"반전세"는 별도 값 없음** → 아래 정의 참고 |
| `price` | 매매가 | 숫자(만원) | 매매만 값 있음 |
| `deposit` | 보증금 | 숫자(만원) | 전세·월세만 값 있음. 3억 = `30000` |
| `monthly_rent` | 월세 | 숫자(만원) | 월세만 값 있음. 전세는 공란 |
| `price_per_pyeong` | 평당가 | 숫자(만원) | 매매만 값 있음 |

### 파생 규칙
- **전세**: `deal_type == "전세"`
- **반전세**: `deal_type == "월세"` AND `monthly_rent > 0` AND `deposit >= 10000` (보증금 1억 이상) — **확정**
- **월세(순수)**: `deal_type == "월세"` AND `deposit < 10000` → 이번 버전 표시 제외
- **평(평형)**: `area_m2 / 3.3058`, 소수 1자리
- **전용면적 프리셋**
  - 소형: ~60㎡ (~약 18평)
  - 중소형: 60–85㎡ (약 18–26평)
  - 중형: 85–102㎡ (약 26–31평)
  - (102㎡ 초과는 프리셋 밖 → "전체" 선택 시에만 노출)

### 데이터 한계 (화면에 명시할 것)
- 과거 실거래이지 현재 매물이 아님 → "이 동네가 이 예산대에 거래된다"는 **참고 지표**.
- 계약일이 부정확할 수 있어 **월 단위**로만 신뢰.
- 신축/구축, 동·향, 수리 상태 반영 안 됨.

## 5. 지역 범위

**서남권 4개 구**: 구로구 · 금천구 · 영등포구 · 관악구
(CSV에서 `gu` 기준 필터. 다른 구 데이터는 로드하되 기본 숨김, 향후 확장 대비)

### 동별 인프라 표기 (동 단위 대략 · 수동 매핑)
`data/dong-info.json`에 동별 정적 정보를 표로 넣는다. **초안 작성 완료 → 작성자 검수 필요.**

각 항목 구조:
- `gu`, `dong`, `lat`, `lng` (동 중심 대략 좌표 — 지도 마커 위치용)
- `subway`: 주요 지하철 노선/역 (예: 신도림동 → "1·2호선 신도림, 2호선 문래")
- `note`: 한 줄 특징 (예: "환승 요지, 대형마트·백화점 인접")

영등포구 세부 법정동(당산동1~6가, 문래동2~6가, 양평동1~8가, 영등포동1~8가)은
대표 동(당산동·문래동·양평동·영등포동)으로 **묶어서** 집계·표기한다. → 가공 스크립트에 매핑 규칙 포함.

> 정확한 도보시간·단지별 역세권 판정은 하지 않음. 좌표·문구는 대략치이며 작성자 수기 검수로 확정.

## 6. 기능 요구사항

### 6.1 지도 (핵심 화면)
- FR-1. 서울 서남권이 기본으로 보이는 지도를 표시한다.
- FR-2. 필터를 통과한 실거래를 지도에 표시한다.
  - 방식 A(기본): **동별 집계 마커** — 동 중심에 원, 크기=거래건수, 색=평균 전세보증금 구간
  - 방식 B: 개별 거래 마커 (줌인 시) — 클릭하면 단지명·면적·층·보증금·월세·계약월
- FR-3. 마커/원 클릭 시 팝업: 동 이름, 필터 조건 내 **평균·중위 보증금**, 거래건수, 대표 단지 3개, 지하철 표기.
- FR-4. 예산 판정 색상: 필터의 보증금 상한 대비
  - 초록 = 평균이 상한의 85% 이하 (여유)
  - 노랑 = 85–100%
  - 회색 = 필터 결과 없음

### 6.2 필터 패널
- FR-5. 거래유형: `전세` / `반전세` 체크박스 (기본: 둘 다 on)
- FR-6. 보증금 상한 슬라이더: 1억 ~ 4억, 기본 **3억**, 5백만원 단위
- FR-7. 월세 상한 슬라이더: 0 ~ 200만원, 기본 100만원 (반전세 켜졌을 때만 활성)
- FR-8. 평형 프리셋 버튼: 소형 / 중소형 / 중형 / 전체 (다중 선택, 기본 전체)
- FR-9. 구 선택: 구로 / 금천 / 영등포 / 관악 (다중, 기본 전부)
- FR-10. 기간: 최근 3 / 6 / 12개월 (기본 12개월)
- FR-11. 필터 변경 시 지도·요약이 즉시 갱신.
- FR-12. 필터 상태가 URL 쿼리스트링에 반영되어 **링크 공유 시 같은 화면** 재현.

### 6.3 요약 영역
- FR-13. 현재 필터 기준: 총 거래건수, 동 개수, 전세 평균/중위 보증금, 가장 저렴한 동 Top 3.
- FR-14. 데이터 기간·출처·한계 문구 상시 노출.

### 6.4 반응형
- FR-15. 모바일(폭 ~380px)에서 지도 + 접히는 필터 패널로 동작 (신혼부부가 폰으로 볼 가능성 큼).

## 7. 비기능 요구사항

- NFR-1. **정적 웹페이지**로 동작 (백엔드 없음). 1차: 단일 `index.html` 파일 하나.
- NFR-2. CSV는 빌드 시점에 서남권·전세/월세만 추린 경량 파일(JSON 또는 슬림 CSV)로 가공해 동봉.
  원본 32만 행을 브라우저에서 파싱하지 않는다. (목표: 동봉 데이터 < 1MB)
- NFR-3. 지도 라이브러리: **Leaflet + OpenStreetMap 타일 확정** (키 불필요, 무료). 한글 표기가 부족한 구간은 동별 마커/라벨을 앱에서 직접 얹어 보완.
- NFR-4. 외부 의존성은 CDN 1–2개로 제한, 오프라인에서도 큰 기능 손상 없게.
- NFR-5. 최신 크롬·사파리·엣지 최신 버전 지원.
- NFR-6. 첫 로딩 3초 이내(일반 가정용 인터넷 기준).

## 8. 배포 계획 (차근차근)

| 단계 | 내용 | 산출물 |
|---|---|---|
| 0 | PRD 확정 | `PRD.md` (이 문서) | ✅ |
| 1 | 데이터 가공 스크립트 + 슬림 데이터 파일 | `scripts/build-data.ps1`, `data/southwest-rent.json` (22,809건) | ✅ |
| 2 | **단일 `index.html` 프로토타입** | `index.html` (Leaflet+OSM, 필터·지도·팝업·요약·URL공유·반응형) | ✅ |
| 3 | 동별 인프라 표기 JSON | `data/dong-info.json` (초안 — 작성자 검수 대기) | 🟡 |
| 4 | 모바일 반응형·URL 공유 마무리 | index.html 내 구현 완료, 실기기 확인 필요 | 🟡 |
| 5 | GitHub 저장소 생성 → GitHub Pages 활성화 → 공개 URL | https://jayjunglim2.github.io/apt-dashboard/ | ✅ |

> 로컬 실행은 `file://`로 안 되고 정적 서버 필요 → `scripts/serve.ps1` 제공. 자세한 건 `README.md`.

> 서버·자동 데이터 갱신은 범위 밖. 데이터 업데이트는 "새 CSV 받으면 가공 스크립트 다시 돌려 커밋"으로 수동 처리.

## 9. 화면 구성 (와이어프레임 개요)

```
┌───────────────────────────────────────────────┐
│  서남권 신혼부부 전세 지도       [기간: 12개월 ▾] │
├──────────────┬────────────────────────────────┤
│  필터         │                                │
│  ☑ 전세       │                                │
│  ☑ 반전세     │          [   지 도   ]          │
│  보증금 ≤ 3.0억│      동별 원(색=예산여유도)       │
│  ───●─────    │                                │
│  월세 ≤ 100만 │                                │
│  ───●─────    │                                │
│  평형:        │                                │
│  [소형][중소형]│                                │
│  [중형][전체]  │                                │
│  구: ☑구로 …  │                                │
├──────────────┴────────────────────────────────┤
│ 요약: 거래 1,240건 · 18개 동 · 평균 전세 2.6억   │
│ 저렴한 동 Top3: 독산동 · 시흥동 · 가산동          │
│ ※ 2025.07–2026.06 국토부 실거래. 현재 매물 아님. │
└───────────────────────────────────────────────┘
```

## 10. 열린 질문

1. ~~반전세 판정 임계값~~ → **보증금 1억 이상으로 확정.**
2. ~~동별 인프라 문구 주체~~ → **Claude 초안 작성 완료(`data/dong-info.json`), 작성자 검수 예정.**
3. ~~지도 타일~~ → **OSM(Leaflet) 확정.**
4. 월세 상한을 "환산보증금"으로 통합 비교하는 옵션 → 2차 버전 검토.
5. `data/dong-info.json`의 동 좌표·문구 작성자 검수 (특히 영등포 묶음 처리, 관악 언덕 지형 표현).
6. GitHub Pages 공개 시 데이터 출처/라이선스 표기 문구 확인 (국토부 실거래가 + 원본 저장소 출처).

## 11. 성공 기준

- 필터 몇 번으로 "우리 예산 3억으로 갈 만한 동 3–5곳"을 1분 안에 추린다.
- 부부 둘 다 폰으로 링크 열어서 같은 화면을 본다.
- 다른 신혼부부에게 링크를 보냈을 때 설명 없이 이해한다.
